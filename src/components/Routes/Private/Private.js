const Private = () => {
    {/*const{token}=useContext(StoreContext)
  return (
    <Route>
        {...rest}
        render={()=>token?<Component{...rest}/>:
        <Redirect to="/login"/>}
    </Route>
  )*/}
}

export default Private